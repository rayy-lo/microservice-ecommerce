services:
  app:
    image: micro-ecommerce
    command: sh -c "yarn install && yarn run dev"
    ports:
      - 3000:3000
    working_dir: /app
    volumes:
      - ./pages:/app/pages
      - ./public:/app/public
      - ./styles:/app/styles
